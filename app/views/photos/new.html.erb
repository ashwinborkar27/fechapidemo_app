<h1>New Photo</h1>

<%#= render 'form', photo: @photo %><br>

<%= link_to 'Back', album_photos_path, class:'btn btn-mini btn-dark' %>

<%= render partial: 'form', photo: @photo, locals: { url: album_photos_path, method: :post} %>
